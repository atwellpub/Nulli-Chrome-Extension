/**
 *  Load profiles and run taskss
 */
chrome.storage.sync.get('profiles' , function(obj) {
	console.log("Nulli has loaded " + obj.lenngth + "profiles.");
	console.log(obj);
	
	var Engine = ( function() {
		
		var construct = {
		
		
		
		}
		
		
		return construct;
	
	});
});